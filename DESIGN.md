# DESIGN.md — ai-learn 设计规范（强制）

> 所有新增内容与模板改动必须遵守本规范。原则：**读者永远知道自己在哪、刚学了什么、下一步学什么**。

## 1. 配色规范（语义色锁定，禁止自选）

全部色值已定义于 `templates/shared.mjs` 的 `:root`，**只能用变量，不许新写色值**：

| 变量 | 值 | 语义 |
|---|---|---|
| `--accent` | `#0969da` | 主色：链接/高亮/概念卡（蓝） |
| `--green` | `#1a7f37` | 完成/正确/自测（绿） |
| `--amber` | `#9a6700` | 进行中/注意（黄） |
| `--muted` | `#656d76` | 次要文字/规划中（灰） |
| `--fg` / `--bg` / `--border` / `--card` | `#24292f` / `#fafafa` / `#d8dee4` / `#fff` | 基础面 |

教学卡片语义色（构建器从 blockquote emoji 自动生成，手写 HTML 时照搬类名）：

| 类名 | 触发 | 底色系 | 用途 |
|---|---|---|---|
| `.analogy` | `> 💡` | 黄渐变 | 生活比喻 |
| `.concept` | `> 📌` | 蓝渐变 | 核心概念正名 |
| `.quiz` | `> ✅` | 绿渐变 | 自测题 |
| `.field` | `> 🔬` | 粉渐变 | 实战验证/真实数据 |
| `.warn` | `> ⚠️` | 粉边白底 | 警告/坑 |

自检：内容中的裸色值**只允许出现在 mermaid 代码块内**（§2 语义色），正文/HTML 属性一律用变量：
```bash
# mermaid 块外不应有色值（手动核对 grep 结果是否都在 ```mermaid 段内）
grep -oE '#[0-9a-fA-F]{3,6}' content/*.md
```

## 2. mermaid 规范

- 统一 `theme: neutral`（已内置于初始化，勿改）
- 节点数 ≤ 12；超过就拆两张图
- 高亮语义（与站内进度色一致）：
  - 绿 `fill:#dafbe1,stroke:#1a7f37` = 已完成/重点
  - 黄 `fill:#fff8c5,stroke:#9a6700` = 当前/待讲
  - 灰 `fill:#f6f8fa,stroke:#656d76,stroke-dasharray: 5 5` = 规划中/弱化分支
- 流程方向：主线 `flowchart LR`（学习路径/流水线），层级 `flowchart TD`（结构拆解/包含关系）

## 3. 章节内容规范（教学节奏）

节奏固定（AGENTS.md 铁律 6），另有：

- **开篇**：第一段必须是一张"💡 比喻卡"或一段 ≤3 句的人话总起，不许多论
- **术语纪律**：黑话首次出现必须紧邻一张 `📌 概念正名` 卡（术语+定义+公式逐符号拆）；同一术语全站用词一致（对照 `chapters.json` tags）
- **代码块**：语言标注必填；开头注释标明文件与行号（`# model_minimind.py L154-176`）；每 ≤10 行代码夹一句人话注释；行内代码用 `code` 标签
- **实战卡**：数据必须是真实实验产出（K/M 编号可溯），禁止编造示意数值；图片一律 `/assets/` 绝对路径
- **自测卡**：3 问，考术语不考记忆；**答案用 `<details>` 折叠**（见 §5）
- **章末**："下一步"一句话预告 + 侧栏自然衔接

## 4. 导航与布局

- 章节页自动带：侧栏（含进度徽章 ✓/§/…）+ 底部 **prev/next 章链接**（build.mjs 按 chapters.json 顺序生成）
- 每章 ≥1 张 mermaid 或 ≥1 张实战图（纯文字章不合格）
- 移动端单列（已有媒体查询），mermaid 容器允许横向滚动

## 5. 互动元素

| 元素 | 语法 | 用途 |
|---|---|---|
| 折叠答案 | `<details><summary>参考答案</summary>…</details>` | 自测答案、选读细节、证明过程 |
| Playground | `dist/playground/{slug}.html` 单文件，章节页 `.btn-play` 入口 | 可交互演示的核心机制 |

`<details>` 默认样式已在 shared.mjs 定义（无 JS、移动端友好）。自测答案、公式推导、拓展阅读一律折叠——**渐进披露**：主线轻、纵深可选。

## 6. Playground 规范（可选，不强制）

**判定标准（满足才做，不硬凑）**：概念有"可即时反馈的核心机制"——用户拨动参数 → 立刻看到结果变化。

- 适合：MoE 路由（调 gate 看 aux_loss/负载）、temperature 采样（调 T 看分布）、CE loss（改概率看惊讶度）、SFT loss mask（选 token 看哪些计入 loss）
- 不适合：纯概念叙述（RoPE 推导）、纯流程（训练循环）
- 技术约束：**单文件 HTML、内联 CSS/JS、零网络依赖、纯前端**；深色工作台风（`--bg:#0d1117` 系），与阅读页（浅色）区分——"读是读，玩是玩"
- 章节页入口：`.btn-play` 按钮 + `/playground/{slug}.html` 链接
- 完成后必须校验：标签平衡 + sandbox_exec 或本地浏览器跑通交互逻辑
- 标杆：`dist/playground/moe-router.html`（本站首个）

## 7. 文字风格（极简铁律）

### 7.1 短句
- 一句一个信息点，**≤25 字为宜**
- 段落 ≤3 行；列表项一句话
- 长句拆短句，禁止套从句

### 7.2 禁填充词
删掉不改意思的词：
- ❌ 其实 / 实际上 / 值得注意的是 / 也就是说 / 换句话说
- ❌ 请想象一下 / 我们可以把它看作 / 不难发现 / 可以看到
- ❌ 显然 / 易得 / 不难证明
- ❌ "这就是……的全部野心" 式总结腔

### 7.3 术语双语（强制格式）
英文概念**首次出现**必须中英对照：
- ✅ `Attention（注意力机制）`、`GQA（分组查询注意力）`、`KV Cache（键值缓存）`、`epoch（训练轮数）`
- 之后可用英文或中文简称
- 标题/卡片标题同样遵守

### 7.4 图优先层级
表达同一信息，选上层：
```
SVG 示意图 / mermaid 图  >  表格  >  列表  >  段落
```
- 每节先图后文；一段超 3 行，先问"能否改成图/表"
- 流程用 mermaid；结构对比用表；数值变化用图

### 7.5 比喻三律（防加深误解）
默认**不用比喻**。概念解释首选**具体示例**：输入 X → 输出 Y → 原因 Z。
比喻只在满足三律时用：
1. **单域律**：一张卡只用一个比喻域。混搭即违例（❌"主题乐园：数据=食材、Pretrain=刀工"——乐园里没有食材）
2. **单点律**：一个比喻只映射一个概念。不延伸剧情、不二次造词
3. **通识律**：日常通识场景（医院分诊、开会、查字典）。拿不准贴切性 → 弃用比喻，改示例

合格样例：`Attention = 开会时决定听谁说`（单点）、`MoE = 医院分诊台`（单域）
违例样例：主题乐园混搭厨房、把 fp16 叫"省墨模式"（打印机墨 ≠ 数值精度）
比喻卡 ≤3 短句 + 1 句点题。
